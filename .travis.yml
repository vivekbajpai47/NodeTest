language: node_js

# test on two node.js versions: 0.6 and 0.8
node_js:
  - 0.6
  - 0.8

# configure notifications (email, IRC, campfire etc)
# please update this section to your needs!
notifications:
  irc: "irc.freenode.org#travis"
deploy:
   edge: true
   provider: bluemixcloudfoundry
   username: vivek_bajpai@persistent.co.in
   password: Cisco@231
   organization: vivek_persistent
   space: vivek_persistent
   manifest: manifest-prod.yml          # (optional)  Defaults to manifest.yml.
   region: au-syd                        # (optional)  [ng, eu-gb , au-syd] Defaults to US South region (ng).
   api: https://api.ng.bluemix.net      # (optional)  Overrides region setting if specifed for Bluemix local installations.
